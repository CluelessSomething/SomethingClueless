public class Plane extends Vehicle {

    public Plane(String type, String powerType, int numWheels, int numDoors, String color, double fuelEconomy,
            int numPassangers, int passengerCapacity) {
        super("plane", "electric", 3, 2, "black and pretty golf", fuelEconomy, 4, 4);

    }
    // Make it a vehicle
    // Make a variable for whether the landing gear is down
    // Make a takeoff Function
    //
}
