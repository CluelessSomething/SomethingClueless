import java.util.Scanner;
import java.util.function.Function;

public class TicTakToe {
    public static void main(String... args) {
        Scanner scanner = new Scanner(System.in);
        char[][] board = new char[][] {
                new char[3],
                new char[3],
                new char[3],
        };
        System.out.println("Pick a number 1, 2, or 3");
        double row = scanner.nextDouble();
        double column = scanner.nextDouble();
        double games = scanner.nextDouble();
        double row = new double[rows];

        if (row == 1 | column == 1) {
        }
        if (row == 1 | column == 2) {
        }
        if (row == 1 | column == 3) {
        }
        if (row == 2 | column == 1) {
        }
        if (row == 2 | column == 2) {
        }
        if (row == 2 | column == 3) {
        }
        if (row == 3 | column == 1) {
        }
        if (row == 3 | column == 2) {
        }
        if (row == 3 | column == 3) {
        }
    }
    // write a func to get a number b/w 1 and 3 from user
    // write function that checks a board to see if it has three rows
    // write a function to print the board
    // write a funtcion that takes a scanner, a board, and either 'X' or 'O'. Have
    // it ask for
    // 2 numbers for row and column, and put that letter into that square (or ask
    // again if it's occupied)
}

// _ | _ | _
// _ | _ | _
// | |
